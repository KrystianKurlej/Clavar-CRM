# Deny listing and restrict direct PHP entry (Apache)
Options -Indexes
<IfModule mod_rewrite.c>
RewriteEngine On
# Route everything to index.php (except existing files)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
</IfModule>
